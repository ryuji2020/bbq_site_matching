<% relationship = Relationship.find_by(follower_id: current_user.id, followed_id: user.id) %>
<%= link_to relationship_path(relationship), method: :delete, remote: true do %>
  <button class="btn btn-primary">フォロー中</button>
<% end %>
