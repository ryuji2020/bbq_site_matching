<% provide(:title, 'Notification') %>
<% provide(:page_title, '通知一覧') %>

<div class="py50">
  <% if @notifications.present? %>
    <%= render @notifications %>
  <% else %>
    <p>通知はありません</p>
  <% end %>
</div>
